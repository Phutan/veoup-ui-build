VeoUp UI (GitHub-ready build)

HƯỚNG DẪN:
1️⃣ Tạo repo mới trên GitHub (public hoặc private đều được)
2️⃣ Upload toàn bộ thư mục này (hoặc `git push` nếu quen)
3️⃣ Vào tab "Actions" → workflow "Build VeoUp UI (Windows portable EXE)" sẽ tự chạy
4️⃣ Khi hoàn tất → tải artifact "VeoUp-Windows-Portable" → giải nén, chạy "VeoUp UI.exe"
